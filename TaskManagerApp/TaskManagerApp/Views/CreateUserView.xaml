<UserControl x:Class="TaskManagerApp.Views.CreateUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskManagerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="350"
             Width="350" Height="300">
    <Border Background="White" CornerRadius="15" BorderBrush="#DCDDE1" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3"/>
        </Border.Effect>

        <StackPanel Margin="30">
            <TextBlock Text="Add New Student" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <Label Content="Username" Foreground="Gray"/>
            <TextBox x:Name="UsernameBox" Height="35" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

            <Label Content="Password" Foreground="Gray"/>
            <PasswordBox x:Name="PasswordBox" Height="35" Margin="0,0,0,25" VerticalContentAlignment="Center"/>

            <Button Content="Create Account" Click="Submit_Click" 
                    Background="#27AE60" Foreground="White" Height="40" FontWeight="Bold" Cursor="Hand"/>

            <Button Content="Cancel" Click="Cancel_Click" 
                    Background="Transparent" Foreground="Gray" BorderThickness="0" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
