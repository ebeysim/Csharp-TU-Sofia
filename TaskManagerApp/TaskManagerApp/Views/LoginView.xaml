<Window x:Class="TaskManagerApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    
    <StackPanel Margin="30" VerticalAlignment="Center">
        <TextBlock Text="Username" FontSize="14" FontWeight="Bold" />
        <TextBox x:Name="UserInput" Height="35" Padding="10" FontSize="12" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Password" FontSize="14" FontWeight="Bold" />
        <PasswordBox x:Name="PasswordInput" Height="35" Padding="10" FontSize="12" />

        <Button Content="Login" Click="Login" Height="40" Margin="0,10,0,0" FontSize="14" />

        <Label Content="{Binding ErrorMessage}" Foreground="Red" FontSize="12" />
    </StackPanel>
</Window>
